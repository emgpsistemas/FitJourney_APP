import { MagnifyingGlass } from "phosphor-react-native";
import { TouchableOpacity } from "react-native";

export function InputSeachIcon() {
  return (
    <TouchableOpacity>
      <MagnifyingGlass size={20} />
    </TouchableOpacity>
  );
}
